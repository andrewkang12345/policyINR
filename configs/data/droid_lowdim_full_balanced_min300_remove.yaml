name: droid_lowdim_full_balanced_min300_remove
kind: droid
source: droid
data_dir: ${oc.env:INR_DROID_CACHE,${oc.env:HOME}/.cache/INR/droid}
# Balanced snapshot from the 707/2048-shard DROID min300 scan:
# no unknown task families, 116 episodes per collector, and remove as OOD.
max_shards: null
max_episodes: 1000000
n_collectors: 3
collectors:
  - RAIL
  - IRIS
  - CLVR
min_episodes_per_collector: 8
min_length: 300
max_length: null
ood_task_family: remove
